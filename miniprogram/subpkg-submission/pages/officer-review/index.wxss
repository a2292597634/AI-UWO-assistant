.review-page {
  box-sizing: border-box;
  width: 100%;
  height: 100vh;
  padding: 0 var(--uwo-space-4) calc(var(--uwo-space-12) + env(safe-area-inset-bottom));
  background: var(--uwo-color-canvas);
  color: var(--uwo-color-text-primary);
  font-family: var(--uwo-font-family-body);
}

.review-header {
  padding: var(--uwo-space-8) var(--uwo-space-2) var(--uwo-space-6);
}

.review-header__copy {
  display: flex;
  min-width: 0;
  flex-direction: column;
  gap: var(--uwo-space-2);
}

.review-header__eyebrow {
  color: var(--uwo-color-accent-text);
  font-size: var(--uwo-font-size-supporting);
  font-weight: 600;
  letter-spacing: 2rpx;
}

.review-header__title {
  color: var(--uwo-color-ink);
  font-size: var(--uwo-font-size-page-title);
  font-weight: 700;
  line-height: 1.2;
  overflow-wrap: anywhere;
}

.review-header__hint,
.message-panel__text {
  color: var(--uwo-color-text-secondary);
  font-size: var(--uwo-font-size-supporting);
  line-height: 1.5;
  overflow-wrap: anywhere;
}

.review-header__count {
  color: var(--uwo-color-danger);
  font-size: var(--uwo-font-size-body);
  font-weight: 700;
  overflow-wrap: anywhere;
}

.review-tabs {
  width: 100%;
  margin-bottom: var(--uwo-space-4);
  border-bottom: 2rpx solid var(--uwo-color-border-subtle);
  background: var(--uwo-color-surface);
  white-space: nowrap;
}

.review-tabs__content {
  display: flex;
  min-width: max-content;
}

.review-tab {
  display: flex;
  min-width: 168rpx;
  min-height: 88rpx;
  align-items: center;
  justify-content: center;
  padding: 0 var(--uwo-space-4);
  border-bottom: 6rpx solid transparent;
  color: var(--uwo-color-text-secondary);
  font-size: var(--uwo-font-size-body);
  font-weight: 600;
  text-align: center;
}

.review-tab--active {
  border-bottom-color: var(--uwo-color-accent-brass);
  color: var(--uwo-color-accent-text);
}

.review-message,
.message-panel {
  display: flex;
  min-width: 0;
  flex-direction: column;
  align-items: center;
  gap: var(--uwo-space-3);
  margin-bottom: var(--uwo-space-4);
  padding: var(--uwo-space-8) var(--uwo-space-6);
  border: 2rpx solid var(--uwo-color-border-subtle);
  border-radius: var(--uwo-radius-card);
  background: var(--uwo-color-surface);
  color: var(--uwo-color-text-secondary);
  font-size: var(--uwo-font-size-body);
  text-align: center;
}

.message-panel--error {
  align-items: flex-start;
  border-color: var(--uwo-color-danger);
  text-align: left;
}

.message-panel__title {
  color: var(--uwo-color-ink);
  font-size: var(--uwo-font-size-section-title);
  font-weight: 700;
}

.message-panel__action {
  width: 100%;
  margin: var(--uwo-space-2) 0 0;
}

.review-card {
  min-width: 0;
  margin-bottom: var(--uwo-space-3);
  padding: var(--uwo-space-4);
  border: 2rpx solid var(--uwo-color-border-subtle);
  border-radius: var(--uwo-radius-card);
  background: var(--uwo-color-surface);
}

.review-card:active {
  opacity: 0.88;
  transform: translateY(2rpx);
}

.review-card__heading,
.review-card__title-wrap,
.review-card__meta {
  display: flex;
  min-width: 0;
  align-items: center;
}

.review-card__heading {
  justify-content: space-between;
  gap: var(--uwo-space-3);
}

.review-card__title-wrap {
  flex: 1;
  flex-wrap: wrap;
  gap: var(--uwo-space-2);
}

.review-card__title {
  max-width: 100%;
  color: var(--uwo-color-ink);
  font-size: var(--uwo-font-size-emphasis);
  font-weight: 700;
  overflow-wrap: anywhere;
}

.review-card__revision,
.review-card__meta {
  color: var(--uwo-color-text-secondary);
  font-size: var(--uwo-font-size-supporting);
}

.review-card__meta {
  flex-wrap: wrap;
  gap: var(--uwo-space-2) var(--uwo-space-4);
  margin-top: var(--uwo-space-2);
}

.review-card__reason {
  display: flex;
  min-width: 0;
  flex-direction: column;
  gap: var(--uwo-space-1);
  margin-top: var(--uwo-space-3);
  padding: var(--uwo-space-3);
  border-left: 4rpx solid var(--uwo-color-danger);
  background: var(--uwo-color-surface-muted);
}

.review-card__reason-label {
  color: var(--uwo-color-danger);
  font-size: var(--uwo-font-size-minimum-action);
  font-weight: 600;
}

.review-card__reason-text {
  color: var(--uwo-color-text-primary);
  font-size: var(--uwo-font-size-body);
  line-height: 1.5;
  overflow-wrap: anywhere;
}

.review-card__next {
  display: block;
  margin-top: var(--uwo-space-3);
  color: var(--uwo-color-accent-text);
  font-size: var(--uwo-font-size-supporting);
  font-weight: 600;
  text-align: right;
  overflow-wrap: anywhere;
}

.page-bottom-spacer {
  height: var(--uwo-space-8);
}

@media screen and (max-width: 680rpx) {
  .review-card__heading {
    align-items: flex-start;
    flex-direction: column;
  }
}
