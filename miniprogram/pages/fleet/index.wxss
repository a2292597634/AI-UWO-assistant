/* ── Config Modal (shared) ── */

.config-modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.55);
  z-index: 200;
  display: flex;
  align-items: center;
  justify-content: center;
}

.config-modal {
  background: #2f302b;
  border-radius: 16rpx;
  width: 600rpx;
  max-height: 80vh;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

.config-modal--small {
  width: 520rpx;
}

.config-modal__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20rpx 24rpx;
  border-bottom: 1rpx solid #3d3e38;
  flex: 0 0 auto;
}

.config-modal__title {
  font-size: 30rpx;
  font-weight: 600;
  color: #e0d8c0;
}

.config-modal__close {
  font-size: 28rpx;
  color: #8a8a7e;
  padding: 4rpx;
}

.config-modal__body {
  padding: 24rpx;
  flex: 1;
  overflow-y: auto;
}

.config-modal__text {
  font-size: 26rpx;
  color: #c0b890;
  line-height: 1.6;
  margin-bottom: 24rpx;
}

.config-modal__input {
  width: 100%;
  padding: 16rpx;
  background: #3a3b35;
  border: 1rpx solid #4a4b45;
  border-radius: 8rpx;
  color: #e0d8c0;
  font-size: 28rpx;
  box-sizing: border-box;
  margin-bottom: 24rpx;
}

.config-modal__actions {
  display: flex;
  gap: 16rpx;
}

.config-modal__actions--stack {
  flex-direction: column;
}

.config-modal__btn {
  flex: 1;
  padding: 16rpx;
  border-radius: 8rpx;
  font-size: 26rpx;
  text-align: center;
  border: none;
}

.config-modal__btn--confirm {
  background: #4a6b2e;
  color: #e0d8c0;
}

.config-modal__btn--cancel {
  background: #3a3b35;
  color: #a0a090;
}

.config-modal__btn--danger {
  background: #6b3a2e;
  color: #e0c8c0;
}

/* ── Config List ── */

.config-modal__list {
  flex: 1;
  overflow-y: auto;
}

.config-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20rpx 24rpx;
  border-bottom: 1rpx solid #3a3b35;
}

.config-item:last-child {
  border-bottom: none;
}

.config-item--active {
  background: #3a3b35;
}

.config-item__info {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
  flex: 1;
  overflow: hidden;
}

.config-item__name {
  font-size: 28rpx;
  color: #e0d8c0;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.config-item__meta {
  font-size: 20rpx;
  color: #7a7a6e;
}

.config-item__current {
  font-size: 22rpx;
  color: #5b8c3e;
  flex: 0 0 auto;
  margin-left: 16rpx;
}

.config-modal__empty {
  padding: 40rpx;
  text-align: center;
  font-size: 26rpx;
  color: #7a7a6e;
}

.asset-loading-state,
.asset-load-error {
  display: flex;
  flex: 0 0 auto;
  align-items: center;
  justify-content: center;
  padding: 16rpx 32rpx;
  color: #5a5a4e;
  text-align: center;
}

.asset-load-error {
  flex-direction: column;
}

.fleet-page {
  display: flex;
  flex-direction: column;
  height: 100vh;
  overflow: hidden;
  color: #292a26;
  background: #ded7c7;
}

.fleet-context {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--uwo-space-3);
  padding: var(--uwo-space-3) var(--uwo-space-4);
  border-bottom: 2rpx solid var(--uwo-color-border-subtle);
  background: var(--uwo-color-surface);
  color: var(--uwo-color-text-primary);
}

.fleet-context__copy {
  display: flex;
  min-width: 0;
  flex-direction: column;
  gap: var(--uwo-space-1);
}

.fleet-context__eyebrow,
.fleet-context__title,
.fleet-context__count {
  display: block;
  overflow-wrap: anywhere;
}

.fleet-context__eyebrow,
.fleet-context__count {
  color: var(--uwo-color-text-secondary);
  font-size: var(--uwo-font-size-supporting);
}

.fleet-context__title {
  font-size: var(--uwo-font-size-emphasis);
  font-weight: 600;
}

.fleet-scroll {
  min-height: 0;
  flex: 1;
}

.ship-tabs {
  width: 100%;
  background: #e8dfce;
  border-bottom: 1rpx solid #c4b99a;
}

.ship-tabs__content {
  display: flex;
  width: 1120rpx;
  min-width: 1120rpx;
}

.ship-tab {
  box-sizing: border-box;
  width: 160rpx;
  padding: 16rpx 8rpx 14rpx;
  color: #756b59;
  text-align: center;
}

.ship-tab--active {
  color: #292a26;
  background: #d4cbb4;
  border-bottom: 5rpx solid #b99552;
}

.ship-tab__label,
.ship-tab__meta {
  display: block;
}

.ship-tab__label {
  font-size: 25rpx;
  font-weight: 700;
}

.ship-tab__meta {
  margin-top: 4rpx;
  font-size: 18rpx;
}

.ship-editor {
  padding-bottom: 48rpx;
}

.panel-heading {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.panel-heading__title,
.panel-heading__hint {
  display: block;
}

.panel-heading__hint {
  color: #756b59;
  font-size: 21rpx;
}

.slot-grid {
  display: grid;
  grid-template-columns: repeat(5, minmax(0, 1fr));
  gap: 6rpx;
  padding: 0 14rpx 10rpx;
}

.officer-slot {
  position: relative;
  box-sizing: border-box;
  min-height: 180rpx;
  padding: 10rpx 2rpx;
  overflow: hidden;
  text-align: center;
  background: #e8dfce;
  border: 1rpx solid #c4b99a;
  border-radius: 8rpx;
}

.officer-slot--filled {
  background: #d4cbb4;
}

.officer-slot__number {
  position: absolute;
  top: 3rpx;
  left: 4rpx;
  color: #9b8b6d;
  font-size: 12rpx;
}

.officer-slot__visuals {
  position: relative;
  display: inline-block;
  width: 64rpx;
  height: 64rpx;
  background: #2f302b;
  border-radius: 8rpx;
}

.officer-slot__frame {
  position: absolute;
  top: 0;
  left: 0;
  z-index: 1;
  width: 64rpx;
  height: 64rpx;
  border-radius: 8rpx;
}

.officer-slot__portrait {
  position: absolute;
  top: 0;
  left: 0;
  z-index: 2;
  width: 64rpx;
  height: 64rpx;
  border-radius: 8rpx;
}

.officer-slot__rarity-icon {
  position: absolute;
  top: 0;
  left: 0;
  z-index: 3;
  width: 42rpx;
  height: 42rpx;
}

.officer-slot__type-icon {
  position: absolute;
  bottom: 0;
  left: 0;
  z-index: 4;
  width: 17rpx;
  height: 17rpx;
}

.officer-slot__name,
.officer-slot__empty {
  display: block;
  overflow: hidden;
  margin-top: 2rpx;
  font-size: 14rpx;
  line-height: 1.25;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.officer-slot__empty {
  padding-top: 33rpx;
  color: #a2967d;
}

.ban-chip__remove {
  color: var(--uwo-color-danger);
}

.target-panel,
.candidate-panel,
.ban-panel,
.summary-panel,
.overview-panel {
  margin: 0 20rpx 18rpx;
  padding: 18rpx;
  background: #e8dfce;
  border: 1rpx solid #c4b99a;
  border-radius: 8rpx;
}

.panel-heading {
  margin-bottom: 12rpx;
}

.panel-heading__title {
  font-size: 26rpx;
  font-weight: 700;
}

.target-row {
  display: flex;
  min-height: 88rpx;
  align-items: center;
  padding: var(--uwo-space-3) 0;
  border-bottom: 1rpx solid var(--uwo-color-border-subtle);
}

.target-row__skill {
  display: flex;
  align-items: center;
  min-width: 0;
  flex: 1;
}

.target-row__icon,
.summary-row__icon {
  width: 42rpx;
  height: 42rpx;
  margin-right: 10rpx;
}

.target-row__name {
  overflow: hidden;
  font-size: var(--uwo-font-size-body);
  text-overflow: ellipsis;
  white-space: nowrap;
}

.level-input {
  box-sizing: border-box;
  width: 88rpx;
  min-height: 88rpx;
  margin: 0 var(--uwo-space-2);
  padding: var(--uwo-space-2);
  color: var(--uwo-color-surface);
  background: var(--uwo-color-success);
  border: 0;
  border-radius: var(--uwo-radius-control);
  font-size: var(--uwo-font-size-body);
  text-align: center;
}

.target-row__remove {
  display: flex;
  min-height: 88rpx;
  flex: 0 0 auto;
  align-items: center;
  margin: 0;
  padding: 0 var(--uwo-space-2);
  color: var(--uwo-color-text-primary);
  font-size: var(--uwo-font-size-minimum-action);
  overflow-wrap: anywhere;
  white-space: normal;
}

.target-actions {
  display: flex;
  gap: var(--uwo-space-2);
  margin-top: var(--uwo-space-3);
}

.target-actions .ui-button {
  min-width: 0;
  flex: 1;
  min-height: 88rpx;
  height: auto;
  margin: 0;
  padding: var(--uwo-space-3) var(--uwo-space-4);
  line-height: 1.3;
  font-size: var(--uwo-font-size-body);
  overflow-wrap: anywhere;
}

.target-panel__notice {
  display: block;
  margin-top: var(--uwo-space-3);
  color: var(--uwo-color-warning);
  font-size: var(--uwo-font-size-supporting);
  line-height: 1.4;
  overflow-wrap: anywhere;
}

.ban-list,
.summary-row__contributors {
  width: 100%;
}

.ban-list__content,
.summary-row__contributors-content {
  display: flex;
  align-items: center;
}

.candidate-row,
.overview-row {
  display: flex;
  align-items: center;
  border-bottom: 1rpx solid #d4cbb4;
}

.candidate-row__copy,
.summary-row__copy {
  min-width: 0;
  flex: 1;
}

.candidate-row__name,
.candidate-row__meta,
.candidate-row__status,
.summary-row__name,
.summary-row__level {
  display: block;
}

.candidate-row__name,
.summary-row__name {
  overflow: hidden;
  font-size: 23rpx;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.candidate-row__meta,
.candidate-row__status,
.summary-row__level {
  margin-top: 3rpx;
  color: #756b59;
  font-size: 19rpx;
}

.candidate-row {
  min-height: 88rpx;
  gap: var(--uwo-space-3);
  padding: var(--uwo-space-2) 0;
  border-bottom-color: var(--uwo-color-border-subtle);
}

.candidate-row__visuals,
.ban-chip__visuals {
  position: relative;
  flex: 0 0 auto;
  width: 54rpx;
  height: 54rpx;
  margin-right: 12rpx;
  background: #2f302b;
  border-radius: 7rpx;
}

.candidate-row__frame,
.ban-chip__frame {
  position: absolute;
  top: 0;
  left: 0;
  z-index: 1;
  width: 54rpx;
  height: 54rpx;
  border-radius: 7rpx;
}

.candidate-row__portrait,
.ban-chip__portrait {
  position: absolute;
  top: 0;
  left: 0;
  z-index: 2;
  width: 54rpx;
  height: 54rpx;
  border-radius: 7rpx;
}

.candidate-row__rarity-icon,
.ban-chip__rarity-icon {
  position: absolute;
  top: 0;
  left: 0;
  z-index: 3;
  width: 35rpx;
  height: 35rpx;
}

.candidate-row__type-icon,
.ban-chip__type-icon {
  position: absolute;
  bottom: 0;
  left: 0;
  z-index: 4;
  width: 14rpx;
  height: 14rpx;
}

.contributor-avatar {
  width: 54rpx;
  height: 54rpx;
  margin-right: 12rpx;
  border-radius: 7rpx;
}

.candidate-row__status {
  max-width: 180rpx;
  margin-left: 10rpx;
  color: var(--uwo-color-accent-text);
  font-size: var(--uwo-font-size-minimum-action);
  overflow-wrap: anywhere;
  text-align: right;
}

.candidate-row__contribution {
  display: block;
  margin-top: var(--uwo-space-1);
  color: var(--uwo-color-success);
  font-size: var(--uwo-font-size-supporting);
  font-weight: 600;
  overflow-wrap: anywhere;
}

.ban-list__content {
  gap: 10rpx;
  min-width: max-content;
}

.ban-chip {
  display: flex;
  align-items: center;
  padding: 7rpx 9rpx;
  background: #d4cbb4;
  border-radius: 7rpx;
}

.ban-chip__portrait {
  margin-right: 6rpx;
}

.ban-chip__name,
.ban-chip__remove {
  font-size: 19rpx;
}

.ban-chip__remove {
  margin-left: 7rpx;
}

.summary-row {
  display: grid;
  grid-template-columns: 42rpx minmax(0, 1fr) auto;
  gap: 6rpx 8rpx;
  align-items: center;
  padding: 7rpx 0;
  border-bottom: 1rpx solid #d4cbb4;
}

.summary-row__icon {
  margin-right: 0;
}

.summary-row__contributors {
  grid-column: 2 / 4;
  height: 62rpx;
}

.summary-row__contributors-content {
  min-width: max-content;
  height: 62rpx;
}

.contributor-avatar {
  width: 48rpx;
  height: 48rpx;
  margin-right: 6rpx;
}

.contributors-empty {
  color: #9b8b6d;
  font-size: 20rpx;
}

.overview-row {
  padding: 11rpx 0;
}

.overview-row__name {
  width: 120rpx;
  font-size: 23rpx;
  font-weight: 700;
}

.overview-row__count {
  width: 110rpx;
  color: #756b59;
  font-size: 21rpx;
}

.overview-row__status {
  color: #596257;
  font-size: 20rpx;
}

.ban-panel,
.summary-panel,
.overview-panel {
  margin: 0;
  padding: 0;
  border: 0;
  border-radius: 0;
  background: transparent;
}

.excluded-list {
  display: flex;
  flex-direction: column;
  gap: var(--uwo-space-2);
}

.excluded-row {
  display: flex;
  min-height: 88rpx;
  align-items: center;
  justify-content: space-between;
  gap: var(--uwo-space-3);
  border-bottom: 2rpx solid var(--uwo-color-border-subtle);
}

.excluded-row__name {
  min-width: 0;
  color: var(--uwo-color-text-primary);
  font-size: var(--uwo-font-size-body);
  overflow-wrap: anywhere;
}

.excluded-row__remove {
  flex: 0 0 auto;
  min-height: 88rpx;
  margin: 0;
  padding: 0 var(--uwo-space-3);
  border-radius: var(--uwo-radius-control);
  background: var(--uwo-color-surface-muted);
  color: var(--uwo-color-danger);
  font-family: var(--uwo-font-family-body);
  font-size: var(--uwo-font-size-minimum-action);
  white-space: normal;
  overflow-wrap: anywhere;
}

.excluded-row__remove::after {
  border: 0;
}
