# Uncharted Waters Origin 航海士资料查询小程序设计规格

日期：2026-07-28
状态：已完成讨论，待用户复核

## 1. 项目目标

第一版建设一个面向《Uncharted Waters Origin》国际服玩家的繁体中文微信小程序，用于查询游戏内全部航海士资料。

核心能力包括：

- 按名称查询航海士。
- 按稀有度、职业、性别、语言、主动技能和被动技能筛选。
- 支持多条件组合筛选。
- 通过技能反查拥有该技能的航海士。
- 查看航海士完整资料、头像以及技能图标。

首批用户主要位于中国大陆和台湾。

## 2. 第一版范围

### 2.1 包含

- 全部航海士的基础资料。
- 稀有度、职业、性别、语言等分类资料。
- 主动技能、被动技能及技能图标。
- 名称搜索和多条件组合筛选。
- 航海士列表与详情数据。
- 完全本地化的数据和素材。

### 2.2 不包含

- 队伍配置、编队推荐或数值计算。
- 登录、账号体系和用户云数据。
- 收藏、持有状态或个人标记。
- 在线后台管理系统。
- 自动同步 voyage.tw。
- 服务器、数据库或付费云存储。
- 本地资料管理页面。该功能列入后续版本，但本期需为其预留统一的数据读写和校验边界。

## 3. 核心约束

- 使用原生微信小程序和 TypeScript。
- 界面及正式资料仅支持繁体中文。
- 第一版基础数据经授权后从 voyage.tw 一次性导入。
- 后续更新由项目维护者人工维护，不再依赖 voyage.tw。
- 所有文字、头像和技能图标随小程序本地发布。
- 上线后不得产生服务器、云数据库、云存储或图片流量费用。
- 为满足本地包体要求，可以只保留适合手机显示的图片清晰度，不保留小程序内的高清原图。
- 项目先使用本地 Git，不建立远程仓库。

## 4. 总体架构

数据采用三层架构：

```text
一次性网站导入 ─┐
               ├→ 统一主资料库 → 校验与构建 → 小程序运行数据
未来本地管理页 ─┘

原始网站快照仅用于留档、核对和重新导入。
```

三层职责如下：

1. 原始档案层：保存 voyage.tw 的一次性原始快照，内容不可修改，不进入小程序包。
2. 主资料库层：长期维护的唯一权威数据源，未来所有资料变更均在此进行。
3. 运行数据层：由构建工具自动生成，供小程序读取，不允许人工修改。

## 5. 目录职责

计划采用以下职责划分；实施阶段可在不改变边界的前提下调整具体名称：

```text
archive/
└─ voyage-tw-<snapshot-date>/
   ├─ raw-data/
   ├─ raw-assets/
   └─ source-manifest.json

data/
├─ master/
│  ├─ dataset.json
│  ├─ officers.json
│  ├─ skills.json
│  ├─ dictionaries.json
│  └─ assets.json
└─ schema/

tools/
├─ import/
├─ data-pipeline/
└─ asset-pipeline/

miniprogram/
├─ generated/
│  ├─ catalog/
│  ├─ indexes/
│  ├─ details/
│  └─ asset-manifest/
└─ assets/
```

### 5.1 原始档案

原始档案保存：

- 原始文字资料。
- 原始头像和技能图标。
- 抓取日期和来源网址。
- 原文件哈希值。
- 导入异常和字段映射报告。

原始档案不是未来维护对象。其作用是保留证据、核对导入错误以及在必要时重新运行导入流程。

### 5.2 统一主资料库

`data/master` 是唯一允许人工维护的正式资料库。所有航海士统一保存在 `officers.json`，技能统一保存在 `skills.json`，公共枚举和分类统一保存在 `dictionaries.json`。

未来本地管理页面也只能通过主资料库服务和校验模块读写这一层，不得直接修改小程序生成数据。

### 5.3 小程序运行数据

`miniprogram/generated` 完全由构建工具生成。任何人工修改都会在下一次构建时被覆盖。

## 6. 领域模型

### 6.1 稳定标识

- 每个航海士、技能、职业、语言和素材都拥有项目内部稳定 ID。
- 内部 ID 创建后不得因名称、职业、稀有度或来源字段变化而改变。
- 不使用繁中名称作为实体关联键。
- voyage.tw 的 ID 仅保存于 `sourceRefs`，不作为项目内部关系的唯一依据。

### 6.2 航海士

航海士记录至少包含：

- 稳定 ID。
- 繁中名称。
- 隐藏搜索别名。
- 稀有度 ID。
- 性别 ID。
- 职业 ID。
- 语言 ID 列表。
- 技能引用列表。
- 头像素材 ID。
- 来源引用。
- 可选备注和维护信息。

示意结构：

```json
{
  "id": "officer_000123",
  "name": "航海士名稱",
  "aliases": [],
  "rarityId": "rarity_s",
  "genderId": "gender_female",
  "jobId": "job_xxx",
  "languageIds": ["language_xxx"],
  "skills": [
    {
      "skillId": "skill_0001",
      "kind": "active"
    }
  ],
  "portraitId": "portrait_000123",
  "sourceRefs": {
    "voyageTw": "source-id"
  }
}
```

### 6.3 技能

技能作为独立实体维护，至少包含：

- 稳定 ID。
- 繁中名称。
- 类型和分类。
- 繁中说明。
- 图标素材 ID。
- 来源引用。

技能与航海士的关系保存在航海士记录中，以便关系本身携带主动/被动、槽位或未来可能增加的等级等属性。

### 6.4 字典

稀有度、职业、性别、语言以及其他固定分类通过稳定 ID 维护。显示名称可以调整，但不会破坏航海士引用。

### 6.5 素材

素材清单负责记录：

- 稳定素材 ID。
- 所属实体。
- 原始文件信息。
- 小程序文件路径。
- 文件哈希。
- 宽高和格式。
- 处理规则版本。
- 所属素材分片。

## 7. 数据导入与后续维护

### 7.1 首次导入

首次导入流程：

1. 获取 voyage.tw 的文字和素材快照。
2. 保存来源清单和哈希。
3. 解析原始字段并合并站点语言映射。
4. 建立项目内部稳定 ID。
5. 去重技能和素材。
6. 输出主资料库候选数据。
7. 执行完整校验。
8. 生成差异和异常报告。
9. 人工复核后纳入主资料库。

导入工具是一次性数据接入工具，不会成为小程序运行依赖。

### 7.2 后续人工维护

后续修改只针对 `data/master`：

1. 修改或新增航海士、技能和素材。
2. 运行资料校验。
3. 重新生成查询索引、详情分片和素材清单。
4. 运行测试及包体检查。
5. 在微信开发者工具和实机中验证。
6. 提交本地 Git。

未来本地管理页面将复用同一套读写、校验和构建接口，并提供表单、关联选择、素材预览及保存前校验。

## 8. 查询与筛选

### 8.1 运行时投影

构建工具生成四类运行数据：

- `catalog`：列表所需的轻量航海士摘要。
- `indexes`：名称、稀有度、职业、性别、语言和技能的倒排索引。
- `details`：完整航海士详情，按稳定 ID 分片。
- `asset-manifest`：实体 ID 到本地素材路径和素材分片的映射。

小程序启动时只加载轻量目录和索引，进入详情页时再读取对应详情分片。

### 8.2 组合规则

- 不同筛选类别之间采用“并且”关系。
- 稀有度、职业、性别等单值类别内部多选采用“任一”关系。
- 语言和技能内部多选采用“全部拥有”关系。
- 名称搜索与其他筛选条件共同生效。
- 技能索引支持通过主动或被动技能反查航海士。
- 清除某一条件后仅重新计算结果，不重新读取全部数据。

627 名左右航海士不需要本地数据库或全文搜索引擎。使用紧凑倒排索引和 ID 集合交集即可获得稳定性能。

### 8.3 搜索语言

- 正式展示内容只使用繁体中文。
- `aliases` 可作为隐藏搜索键，为后续支持简称、异体字或简体输入保留能力。
- 是否加入简体搜索别名不影响界面语言，实施时作为独立数据策略确认。

## 9. 本地素材和分包

### 9.1 素材处理

- 原始图片保存在档案层，不进入小程序包。
- 小程序只生成一种适合手机显示的尺寸，列表和详情尽量复用。
- 文件使用稳定 ID 命名，不使用显示名称。
- 自动裁切、缩放、去重和压缩。
- WebP 与优化 PNG 必须经过 Android、iOS 和目标基础库实机验证后确定，不提前写死格式。
- 构建结果记录图片尺寸、格式、哈希和处理规则版本。

### 9.2 分包策略

素材按稳定 ID 分片，不能按职业、稀有度等未来可能变化的属性分片，以避免一次资料调整导致大量文件移动。

正式实现前必须完成一个最小技术实验：

1. 主包搜索页面调用 `wx.loadSubpackage`。
2. 加载指定素材分片。
3. 验证主包页面能否稳定显示该分包中的本地图片。
4. 在开发者工具、Android 和 iOS 上验证。

实验结果决定最终体验：

- 若支持：搜索结果出现时按需加载素材分片。
- 若不支持：列表使用统一占位图，进入详情分包后显示头像。

无论实验结果如何，第一版都不引入远程图片源。

### 9.3 包体预算

构建阶段必须生成包体报告，并设置低于平台硬限制的内部安全预算。以下情况阻止发布：

- 单个分片超出预算。
- 总素材超过预算。
- 出现未分配素材。
- 出现缺失、重复或无法解码的图片。

## 10. 版本与可追溯性

主资料库的 `dataset.json` 至少记录：

- `schemaVersion`：资料结构版本。
- `contentVersion`：内容版本。
- `updatedAt`：资料更新时间。
- `sourceSnapshot`：首次来源快照标识。
- 航海士、技能和素材数量。

构建产物记录输入数据哈希和构建规则版本。相同输入和相同工具版本必须产生相同结果。

本地 Git 负责：

- 保存资料修改历史。
- 比较更新前后的差异。
- 回退错误修改。
- 将资料版本与小程序代码版本对应起来。

## 11. 错误处理

- 解析失败不得静默丢弃记录，必须输出异常报告。
- 未知枚举值不得自动映射到相似名称，必须进入待确认列表。
- 缺失引用、重复 ID、缺失素材和非法技能类型均视为构建错误。
- 非必填内容可以产生警告，但必须被记录。
- 小程序遇到单条详情异常时显示可理解的资料错误状态，不能导致整个列表不可用。
- 素材加载失败时显示统一占位图。

## 12. 开发规范与测试

工程使用：

- 原生微信小程序。
- TypeScript。
- ESLint。
- Prettier。
- 自动化测试。
- 本地 Git。

### 12.1 TDD

所有可独立验证的逻辑遵循：

```text
失败测试 → 最小实现 → 测试通过 → 重构 → 完整验证 → Git 提交
```

优先采用 TDD 的范围：

- 网站数据解析。
- 数据标准化。
- Schema 和关联校验。
- 名称搜索及组合筛选。
- 索引与分片生成。
- 素材命名、去重和预算检查。
- 数据版本和确定性构建。

### 12.2 测试分层

- 单元测试：纯 TypeScript 数据和业务逻辑。
- 数据契约测试：主资料库全部记录及引用关系。
- 构建快照测试：验证确定性输出。
- 组件测试：搜索框、筛选器、列表和详情组件。
- 自动化流程测试：通过微信开发者工具验证核心查询路径。
- 实机验证：至少覆盖 Android 和 iOS。

### 12.3 本地质量命令

计划提供：

```text
npm run lint
npm run format:check
npm run typecheck
npm test
npm run data:check
npm run build:check
```

在建立远程仓库前，这些检查全部在本地执行。未来如接入 GitHub，再增加 CI。

## 13. 实施前技术验证

正式页面开发前必须完成：

1. voyage.tw 全量记录和素材数量审计。
2. 原始字段到领域模型的映射样本。
3. WebP 与优化 PNG 的质量、体积和兼容性比较。
4. 分包素材按需加载实验。
5. Android 和 iOS 的本地图片实机验证。
6. 小程序生成数据的体积和解析性能测量。

只有这些验证通过后，才能确定最终图片尺寸、格式、分片数量和包体预算。

## 14. 已确认的设计决策

- 第一版只做航海士资料查询，不做队伍配置。
- 只提供繁体中文正式内容。
- 全字段可搜索或筛选，并支持组合条件。
- 不保存用户状态。
- 数据从 voyage.tw 一次性导入，之后人工维护。
- 采用原始档案、统一主资料库、运行数据三层架构。
- 主资料库按航海士、技能、字典和素材表统一管理。
- 后续通过本地管理页面维护，但第一版暂不实现。
- 文字和素材全部本地化。
- 接受压缩图片，不保留小程序内高清图。
- 零服务器和零运行费用优先于列表头像体验。
- 业务逻辑和数据工具采用 TDD。

## 15. 成功标准

第一版完成时应满足：

- 全部已导入航海士均可查询。
- 所有已确认字段和关联完整可用。
- 多条件筛选结果符合既定规则。
- 没有运行时网络依赖。
- 没有服务器或云服务固定费用。
- 数据可以通过统一主资料库持续维护。
- 数据和素材构建过程可重复、可校验、可追溯。
- 项目在 Android、iOS 和微信开发者工具中通过核心流程验证。

## 16. UI/UX 已确认设计

### 16.1 视觉语言

- 参考项目根目录中的两张游戏实际截图。
- 主色采用暖灰羊皮纸、炭黑、低饱和酒红、黄铜金和灰橄榄。
- 视觉方向为古典航海资料馆，不使用高饱和现代卡片风。
- 首版先确定信息结构；航海背景插画、罗盘、船帆、海图纹理和其他装饰元素后续深化。
- 所有正式界面文字使用繁体中文。

### 16.2 首页

首页采用 A「模块港口」方案：

- 顶部为应用名称和航海主题标识。
- 首版只显示已经上线的“航海士名鉴”主功能入口。
- 显示资料收录状态、数据版本和最近更新说明。
- 首页不提供航海士快速搜索框。
- 第一版不设置固定底部 Tab Bar。
- 未完成的未来功能不得提前显示。
- 新功能完成后，以模块卡片形式自然加入首页，不破坏现有结构。

### 16.3 航海士名册

名册采用技能优先的连续紧凑资料表：

- 不使用相互分离的大卡片。
- 每行保留小型航海士头像。
- voyage.tw 的原始稀有度素材叠放在头像左上角。
- 姓名下方显示职业和性别摘要。
- 主动技能和被动技能分别使用固定主列，并显示本地技能图标。
- 语言可参与筛选，但不占用名册核心列。
- 每行目标高度约 47px，单屏目标显示约 11 名航海士。
- 点击头像、姓名或资料行空白区域进入航海士详情。
- 点击技能图标直接打开技能底部信息层。

### 16.4 四页式组合筛选

筛选结构参考 voyage.tw，拆分为四个独立页面：

1. 航海士：名称、稀有度、航海类型和性别。
2. 技能：技能名称搜索、折叠分类及主动/被动切换。
3. 语言：独立搜索和多选列表。
4. 职业：独立搜索和多选列表。

交互规则：

- 四个页面通过顶部页签切换。
- 页面切换不得清空已经选择的条件。
- 多个页面的条件可以累计并共同计算结果。
- 稀有度使用原始素材图片，不使用 S、A、B 等替代文字。
- 技能页优先展示技能图标，并支持名称搜索。
- 底部实时显示当前条件对应的航海士数量。
- 应用筛选后返回紧凑名册。

筛选逻辑继续遵守：

- 不同筛选类别之间采用“并且”。
- 稀有度、职业、性别等单值类别内部多选采用“任一”。
- 语言和技能内部多选采用“全部拥有”。

### 16.5 航海士详情

详情页采用 A「技能优先卷宗」方案：

- 顶部紧凑显示头像、稀有度、名称、职业、性别和语言。
- 主动技能与被动技能紧接人物摘要显示。
- 技能图标旁显示技能名称。
- 其他资料放在技能区块之后。
- 所有资料在同一纵向滚动页面中，不依赖分页切换。

### 16.6 技能快速信息

点击技能图标后采用 A「底部快速信息」：

- 不离开当前名册或航海士详情页面。
- 显示技能图标、名称、主动/被动分类、类型和完整说明。
- 提供“查看拥有此技能的航海士”入口。
- 点击反查入口后返回名册并自动应用对应技能条件。

### 16.7 空状态与降级

- 没有符合条件的航海士时保留当前条件，并显示“清除全部筛选”按钮。
- 图片缺失或无法解码时显示统一航海主题占位图。
- 单个素材失败不能影响文字资料查询。
- 单条详情异常不能导致整个名册不可用。